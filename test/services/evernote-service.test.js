const request = require('supertest');
const app = require('../../src/server/services/evernote-service');

describe('evernote-service.js', () => {
  it('example', () => {
    expect(1).toBe(1);
  })
});
